{
  "name": "wasm-image-processor",
  "version": "0.1.0",
  "description": "High-performance image processing using Rust and WebAssembly",
  "type": "module",
  "scripts": {
    "build:wasm": "cargo build --target wasm32-unknown-unknown --release && wasm-bindgen --target web --out-dir pkg target/wasm32-unknown-unknown/release/wasm_image_processor.wasm",
    "dev": "vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.11",
    "vite-plugin-wasm": "^3.3.0",
    "vite-plugin-top-level-await": "^1.4.4"
  },
  "keywords": [
    "wasm",
    "webassembly",
    "rust",
    "image-processing"
  ],
  "author": "Joan Leon",
  "license": "MIT"
}
